# Asistence Frontend (Flutter)

UI mínimo para login y botón de Google.

Requisitos:
- Tener Flutter SDK instalado.

Cómo ejecutar:

```bash
cd Frontend/flutter_app
flutter pub get
flutter run
```

Notas:
- El botón de Google es UI-only; integrar `google_sign_in` y backend luego.
